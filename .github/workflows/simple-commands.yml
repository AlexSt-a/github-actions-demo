name: Simple Commands
on: [push]

jobs:
  builds:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Hello
        run: echo "Hello, World!"

      - name: Show Date
        run: Date
      - name: List Files
        run: ls -la